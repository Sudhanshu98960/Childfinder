# Childfinder
SOS tracker using Arduino, SIM800L &amp; GPS. Sends live Google Maps location to emergency contacts via SMS when SOS button is held. Also replies “WHERE” with current location. LCD + buzzer for status.
